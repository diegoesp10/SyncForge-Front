# true  -> el frontal funciona sin backend (datos simulados)
# false -> usa el backend real a través del proxy /api
VITE_USE_MOCK=true

# URL del backend .NET (solo la usa el proxy de Vite en desarrollo)
VITE_BACKEND_URL=https://localhost:7001

# Tamaño máximo por archivo en MB
VITE_MAX_FILE_MB=50
