# URL del backend .NET (solo la usa el proxy de Vite en desarrollo).
# Perfil "http" de launchSettings.json; con el perfil "https" sería https://localhost:7233
VITE_BACKEND_URL=http://localhost:5254

# Tamaño máximo por archivo en MB
VITE_MAX_FILE_MB=50
